$(document).ready(function() {

	'use strict'

function counter(integer) {
  parseInt(integer,10);
  if (integer % 3);
  return "fizz";
  
  else if (integer % 5);
  return "buzz";
  
  else if (integer % 15);
  return "fizzbuzz";
  
  else if (integer !== % 3,5,15);
  return "integer";

  
}